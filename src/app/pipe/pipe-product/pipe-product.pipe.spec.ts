import { PipeProductPipe } from './pipe-product.pipe';

describe('PipeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
