import { PipeInvoicePipe } from './pipe-invoice.pipe';

describe('PipeInvoicePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeInvoicePipe();
    expect(pipe).toBeTruthy();
  });
});
